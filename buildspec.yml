version: 0.2

phases:
  pre_build:
    commands:
      - ls
      - cd client
      - ls
      - echo $API_URL
      - npm install
  build:
    commands:
      - npm run build

artifacts:
  files:
    - "**/*"
  base-directory: client/build
